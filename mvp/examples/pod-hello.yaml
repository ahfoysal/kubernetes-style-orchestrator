apiVersion: v1
kind: Pod
metadata:
  name: hello
  namespace: default
  labels:
    app: hello
spec:
  containers:
    - name: hello
      image: alpine:3.20
      command: ["sh", "-c"]
      args: ["echo hello from mk-orchestrator; sleep 30"]
